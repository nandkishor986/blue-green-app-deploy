apiVersion: v1
kind: Service
metadata:
  name: bankapp-service
  namespace: webapps
spec:
  type: ClusterIP
  selector:
    app: bankapp
    version: blue # this can be changed to green when switching traffoc. 
  ports:
    - port: 80
      targetPort: 30585
      protocol: TCP



